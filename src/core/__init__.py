"""Core components for the VideoFlow application.

This module contains the fundamental components that power the VideoFlow application,
including the plugin system, logging infrastructure, and plugin interface definitions.

Components:
    - plugin_manager: Manages plugin discovery, loading, and lifecycle
    - plugin_interface: Abstract base class for all plugins
    - logger: Centralized logging system for the application

The core module provides the foundation for the extensible plugin architecture
that allows VideoFlow to be easily extended with new video processing features.
"""
